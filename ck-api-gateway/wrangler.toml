name = "ck-api-gateway"
main = "src/index.js"
compatibility_date = "2024-12-01"

[vars]
AIRTABLE_BASE_ID = "appUSnNgpDkcEOzhN"
ENVIRONMENT = "production"
RATE_LIMIT_RPM = "60"
MAX_TRANSCRIPT_CHARS = "100000"

# ── KV Namespace Bindings ──
# These cache inference results, session state, rate limits, and audit logs

[[kv_namespaces]]
binding = "CACHE"
id = "2a4a09a04ea146b29fa06ebb9af61609"

[[kv_namespaces]]
binding = "SESSIONS"
id = "c88aa8d5534c4b0cb3f2722f97cceca6"

[[kv_namespaces]]
binding = "RATE_LIMITS"
id = "0acfb96a7aaf49bb92055c143a882506"

[[kv_namespaces]]
binding = "AUDIT_LOG"
id = "3d722426db4241209fd0444b42f84904"

# ── Secrets (set via dashboard or `wrangler secret put`) ──
# ANTHROPIC_API_KEY        — Anthropic console API key
# AIRTABLE_API_KEY         — Airtable personal access token
# WORKER_AUTH_TOKEN         — Auth token for external callers (Retell, Zapier)
# SLACK_WEBHOOK_URL         — Slack incoming webhook for notifications
# BANANA_PRO_API_KEY       — Banana Pro AI GPU inference key
# BUFFER_ACCESS_TOKEN       — Buffer social media management OAuth token
# ALPHA_VANTAGE_KEY         — Alpha Vantage financial data API key
# FRED_API_KEY              — Federal Reserve Economic Data API key
# RETELL_WEBHOOK_SECRET     — Retell AI webhook verification secret
