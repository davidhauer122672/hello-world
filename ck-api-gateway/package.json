{
  "name": "ck-api-gateway",
  "version": "1.0.0",
  "description": "Coastal Key API Gateway — Central inference router and orchestration layer",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/__tests__/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
