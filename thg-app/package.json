{
  "name": "thg-app",
  "version": "6.1.0",
  "private": true,
  "description": "Tracey Hunter Group AI-Powered Real Estate App",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "vite build && cp public/manifest.json public/_redirects public/_headers public/sw.js dist/ && npx wrangler pages deploy dist --project-name=thg-app --branch=production"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.0",
    "@testing-library/react": "^16.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "jsdom": "^25.0.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
