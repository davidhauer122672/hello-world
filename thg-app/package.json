{
  "name": "thg-app",
  "version": "6.0.0",
  "private": true,
  "description": "Tracey Hunter Group AI-Powered Real Estate App",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cp public/manifest.json dist/ && npx wrangler pages deploy dist --project-name=thg-app --branch=production"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^6.0.0"
  }
}
